# Development-only dependencies — testing, linting, etc.
# Install with: pip install -r requirements-dev.txt
#
# Production deps are in requirements.txt (must stay minimal for deployment).

pytest>=7.0.0
pytest-cov>=4.0.0  # optional, for coverage reports
